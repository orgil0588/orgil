const Navbar = () => {
  return (
    <nav className="py-4 px-2 md:px-0 bg-transparent backdrop-blur-lg border-b border-[#303030] fixed w-full">
      <div className="container mx-auto">Orgil.</div>
    </nav>
  );
};

export default Navbar;
